# Core runtime dependencies. GPU + plotting extras are intentionally optional
# so the harness installs and its tests pass on a plain CPU box.
torch>=2.1
transformers>=4.40
safetensors>=0.4
huggingface_hub>=0.23

# Distributed fleet (pip install "coldstart-lab[distributed]")
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Optional (install as needed):
#   pip install vllm         # GPU engine experiment
#   pip install matplotlib   # report plots
#   pip install pytest       # test suite
